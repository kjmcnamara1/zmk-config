name: Build Corne ZMK firmware
on:
  workflow_dispatch:
  push:
    paths:
      - "config/corne.*"
      - ".github/workflows/build.yml"
      - "build.yaml"

jobs:
  build-corne:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
